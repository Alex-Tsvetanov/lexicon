% III. Проектиране на системата.
% Структура, договори между слоевете, формати на данните.
\section{Проектиране на системата}
\label{sec:design}

\subsection{Структура на интерфейса и изисквания към него}

Интерфейсът на естествен език е конвейер от четири анализиращи слоя и един синтезиращ.
Входът е низ на естествен език, изходът е формална заявка заедно с перифраза на
разбрания въпрос. Слоевете са морфологичен анализ, синтактичен анализ, семантично
изображение и генериране на код, а синтезът стои встрани от конвейера и се захранва от
семантичното представяне.

Изискванията към интерфейса са четири. Всеки слой приема и връща данни по описан
договор, така че да може да бъде заменен без промяна в останалите. Нееднозначността се
пренася нагоре по конвейера, вместо да се разрешава преждевременно чрез евристика на
ниско ниво. Всяко тълкуване е проследимо назад до конкретните думи, които са го
породили. Заявката не се изпълнява, преди потребителят да е потвърдил перифразата.

\subsection{Морфологичен анализ}

Морфологичният слой е краен преобразувател над лексикона. За всяка входна словоформа
той връща множество от разчитания, всяко от които съдържа лексема, част на речта и
граматични признаци. Множеството е от разчитания, а не единично разчитане, защото
омонимията между словоформи е обичайна и разрешаването ѝ е възможно едва на
синтактично ниво. Форматът на записа в лексикона и наборът от признаци са дадени в
\TODO{таблица с формата на лексикалния запис и набора от граматични признаци}.

\subsection{Синтактичен анализ}

Синтактичният слой приема поредица от множества с разчитания и връща гора от изводи
спрямо граматиката на предметната област. Граматиката описва въпросителните
конструкции, именните и предложните групи, и съгласуването по род и число, което в
силно флективен език отхвърля голяма част от паразитните разчитания още на този етап.
Схемата на конвейера и потокът на данните между слоевете са показани на
\TODO{фигура със структурата на конвейера}.

\subsection{Семантичен анализ}

Семантичният слой изобразява всяко синтактично дърво върху израз в избраното
представяне на знания. Изображението е композиционно: значението на групата се
получава от значенията на съставките ѝ и от правилото, което ги свързва. Разчитанията,
които не се изобразяват върху нищо в модела на знанието, отпадат тук, и точно това
отпадане е основният механизъм за разрешаване на нееднозначността в системата.

\subsection{Генериране на формална заявка}

Генераторът превежда логическата форма в SQL или в SPARQL. Той не съдържа евристики
върху текста: работи изцяло върху вече проверената логическа форма, което е причината
една и съща семантика да може да се насочи към две различни цели без промяна в
предходните слоеве. Съответствието между конструкциите в логическата форма и
конструкциите в двата целеви езика е дадено в \TODO{таблица със съответствието между
логическата форма и целевите езици за заявки}.

\subsection{Синтез на изречение}

Синтезът произвежда изречение на естествен език от семантичното представяне и го
показва на потребителя като перифраза на разбрания въпрос. Ролята му е контролна: ако
системата е избрала грешно разчитане, потребителят вижда това преди изпълнението на
заявката. Реализацията ползва обратната посока на морфологичния преобразувател за
словоизменението и правилата на граматиката за реда на думите, тоест същите два
ресурса, които обслужват и анализа.
